import * as Router from "koa-router";
// import { list, read, update, delete } from '../controllers/db';

const router = new Router();

// router.get("/", list);
// router.get("/:dbId", read);
// router.put("/:dbId", update);
// router.delete("/:dbId", delete);

export { router };
